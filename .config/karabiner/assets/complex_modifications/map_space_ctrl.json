{
  "title": "map SPACE to CTRL(if press with any key)",
  "rules": [
    {
      "description": "スペースが他キーと同時押しされたときは，CTRLとして扱う。単体ならそのまま。",
      "manipulators": [
        { 
          "type": "basic",
          "from": {
            "key_code": "spacebar",
            "modifiers":{"optional":["any"]}
          },
          "to": [ 
            {"key_code": "left_control", "lazy": true}
          ],
          "to_if_alone": [
            {"key_code": "spacebar"}
          ]
        }

      ]
    }
  ]
}
